// import { imgRegex, linkRegex } from "../utils/regex";

// export function useData({ news }) {
//   const img = imgRegex.exec(news)?.[1];
//   const link = linkRegex.exec(news)?.[1];
//   const title = news?.title;
//   return { img, link, title };
// }
